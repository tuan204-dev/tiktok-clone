import React from 'react';
import PropTypes from 'prop-types';

const Search = props => {
  return (
    <div>
      Search Layout
    </div>
  );
};

Search.propTypes = {
  
};

export default Search;